// This file contains the default projects that will always be available in the dashboard
export const DEFAULT_PROJECTS = [
  // Default projects have been removed as requested
  // All projects will now be managed directly from the database
]
